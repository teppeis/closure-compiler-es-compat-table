{
  "name": "closure-compiler-es6-compat-table",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "check": "rm -rf build && node generate-inputs.js && ./compile.sh && node check.js"
  },
  "author": "Teppei Sato",
  "license": "MIT",
  "dependencies": {
    "fast-closure-compiler": "teppeis/fast-closure-compiler#update-closure-compiler-and-nailgun",
    "mkdirp": "^0.5.0",
    "object-assign": "~1.0.0",
    "rimraf": "^2.2.8"
  }
}

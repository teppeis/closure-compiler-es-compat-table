{
  "name": "closure-compiler-es6-compat-table",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "es6": "CL_VERSION=$(./version.sh) ES_VERSION=es6 node generate-inputs.js && ES_VERSION=es6 ./compile.sh && ES_VERSION=es6 ./check.sh",
    "es2016plus": "CL_VERSION=$(./version.sh) ES_VERSION=es2016plus node generate-inputs.js && ES_VERSION=es2016plus ./compile.sh && ES_VERSION=es2016plus ./check.sh",
    "esnext": "CL_VERSION=$(./version.sh) ES_VERSION=esnext node generate-inputs.js && ES_VERSION=esnext ./compile.sh && ES_VERSION=esnext ./check.sh",
    "stop": "./node_modules/closure-gun/nailgun/ng com.martiansoftware.nailgun.builtins.NGStop"
  },
  "author": "Teppei Sato",
  "license": "MIT",
  "dependencies": {
    "closure-gun": "^0.1.8",
    "google-closure-compiler": "^20180319.0.0",
    "mkdirp": "^0.5.0",
    "rimraf": "^2.6.2"
  }
}

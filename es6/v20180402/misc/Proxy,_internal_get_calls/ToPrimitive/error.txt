// misc / Proxy, internal 'get' calls / ToPrimitive
module.exports = function() {
  // ToPrimitive -> Get -> [[Get]]
  var get = [];
  var p = new Proxy(
    { toString: Function() },
    {
      get: function(o, k) {
        get.push(k);
        return o[k];
      }
    }
  );
  p + 3;
  return (
    get[0] === Symbol.toPrimitive && get.slice(1) + "" === "valueOf,toString"
  );
};

----------------------------------------------------------
/Users/teppeis/src/github.com/teppeis/closure-compiler-es6-compat-table/es6/v20180402/misc/Proxy,_internal_get_calls/ToPrimitive/in.js:14: WARNING - Suspicious code. The result of the 'add' operator is not being used.
  p + 3;
  ^^^^^

0 error(s), 1 warning(s)
